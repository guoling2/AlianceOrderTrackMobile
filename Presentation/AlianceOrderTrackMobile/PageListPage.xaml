<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:local="clr-namespace:AlianceOrderTrackMobile.Page"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AlianceOrderTrackMobile.PageListPage">
    <ContentPage.Resources>
        <Style TargetType="Button">
            <Setter Property="HorizontalOptions" Value="Center"></Setter>
            <Setter Property="VerticalOptions" Value="CenterAndExpand"></Setter>
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Button Text="打电话" Clicked="Button_Clicked" />
            <Button Text="LoginDaniaPage"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="{x:Type local:LoginDaniaPage}" />
            <Button Text="MissionTabbedPage"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="{x:Type local:MissionTabbedPage}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>